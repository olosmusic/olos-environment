(function(params){

  // init draggability
  // TO DO: add this to the base element so that everything is draggable

  var oscType = 'sine';
  var freq = 440;

  Polymer('olos-environment', {
    ready: function() {
      console.log('ready!');
    }
  });

})();